### Hibernate Validator tests

This module has custom `instancio.properties` where:

- everything is nullable
- number ranges include negative numbers
- array/collection/map have large max size values

